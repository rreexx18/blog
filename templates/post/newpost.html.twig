{% extends 'base.html.twig' %}
{% block body %}
    {{ parent() }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-success" value="Create">
    {{ form_errors(form) }}
    {{ form_end(form) }}
 
{%endblock%}